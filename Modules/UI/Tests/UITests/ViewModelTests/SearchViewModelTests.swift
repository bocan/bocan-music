import Foundation
import Testing
@testable import Persistence
@testable import UI

// MARK: - SearchViewModelTests

// SearchViewModel does not exist yet — search is handled inline by
// LibraryViewModel.searchQuery.  These tests are placeholders that will be
// enabled once a dedicated SearchViewModel is extracted.

@Suite("SearchViewModel Tests")
@MainActor
struct SearchViewModelTests {
    @Test("placeholder — SearchViewModel not yet extracted")
    func placeholder() {
        // No-op until the type exists.
    }
}

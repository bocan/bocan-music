{
  "name": "bocan-website",
  "version": "0.1.0",
  "private": true,
  "description": "bocan.app — static site for the Bòcan music player",
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve --quiet",
    "clean": "rm -rf _site",
    "check": "node scripts/check-links.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0",
    "markdown-it-anchor": "^9.2.0"
  }
}
